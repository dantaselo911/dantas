(function() {
  'use strict';

  // ====== GABARITO COMPLETO ======
  const gabarito = [
    { "questao_numero": 1, "alternativa_correta": "[IMAGEM] https://edusp-static.ip.tv/tms/edusp/tv.ip.edusp/AaiDkRlOyyjITYDe1ETaY9e6LrN130.jpg" },
    { "questao_numero": 2, "alternativa_correta": "1,09" },
    { "questao_numero": 3, "alternativa_correta": "igual a 8." },
    { "questao_numero": 4, "alternativa_correta": "resolução IV." },
    { "questao_numero": 5, "alternativa_correta": "[IMAGEM] https://edusp-static.ip.tv/tms/edusp/tv.ip.edusp/BbtopN2SvNHSSoKIV5KVuoYjDMYAqA.jpg" },
    { "questao_numero": 6, "alternativa_correta": "R$ 381,00" },
    { "questao_numero": 7, "alternativa_correta": "10" },
    { "questao_numero": 8, "alternativa_correta": "[IMAGEM] data:image/jpeg;base64,/9j/4AAQSk..." },
    { "questao_numero": 9, "alternativa_correta": "1,21" },
    { "questao_numero": 10, "alternativa_correta": "[IMAGEM] https://edusp-static.ip.tv/tms/edusp/tv.ip.edusp/OVxvxi5cmA5JcqLmGCiDXLRf6Ss9hW.jpg" },
    { "questao_numero": 11, "alternativa_correta": "10" },
    { "questao_numero": 12, "alternativa_correta": "[IMAGEM] https://edusp-static.ip.tv/tms/edusp/tv.ip.edusp/1EMDxRK2CPNi8Ho5Wk47TcVd4LGsvf.jpg" },
    { "questao_numero": 13, "alternativa_correta": "[IMAGEM] data:image/jpeg;base64,/9j/4AAQSk..." },
    { "questao_numero": 14, "alternativa_correta": "30" },
    { "questao_numero": 15, "alternativa_correta": "5 20" },
    { "questao_numero": 16, "alternativa_correta": "R$ 2.560,00" },
    { "questao_numero": 17, "alternativa_correta": "Saber quanto se ganha e quanto se pode gastar, evitando desequilíbrios." },
    { "questao_numero": 18, "alternativa_correta": "Planejamento financeiro inadequado." },
    { "questao_numero": 19, "alternativa_correta": "Negociar as dívidas e ajustar o orçamento familiar." },
    { "questao_numero": 20, "alternativa_correta": "Refletir se realmente precisa de algo a mais antes de decidir pela compra." },
    { "questao_numero": 21, "alternativa_correta": "Registro diário e organizado de todos os gastos, inclusive os pequenos." },
    { "questao_numero": 22, "alternativa_correta": "Evitar dívidas e fazer escolhas que respeitem a própria realidade financeira." },
    { "questao_numero": 23, "alternativa_correta": "RNA." },
    { "questao_numero": 24, "alternativa_correta": "realizado no núcleo, é produzida uma fita de RNA a partir do DNA." },
    { "questao_numero": 25, "alternativa_correta": "Tradução, que ocorre nos ribossomos e monta cadeias de aminoácidos." },
    { "questao_numero": 26, "alternativa_correta": "3 verdes : 1 amarela" },
    { "questao_numero": 27, "alternativa_correta": "Clone é um ser ou célula com o mesmo material genético do original, útil na produção de tecidos e no estudo de doenças." },
    { "questao_numero": 28, "alternativa_correta": "Esse tipo de tratamento atua substituindo, inativando ou reparando genes que causam doenças genéticas. É importante que a decisão da família seja baseada em informações acessíveis e compreensíveis." },
    { "questao_numero": 29, "alternativa_correta": "Fonte primária, fonte primária, fonte secundária." },
    { "questao_numero": 30, "alternativa_correta": "900" },
    { "questao_numero": 31, "alternativa_correta": "Refração da luz." },
    { "questao_numero": 32, "alternativa_correta": "Refrator." },
    { "questao_numero": 33, "alternativa_correta": "60°" },
    { "questao_numero": 34, "alternativa_correta": "espelho esférico convexo." },
    { "questao_numero": 35, "alternativa_correta": "o ânodo perde elétrons e o cátodo ganha elétrons." },
    { "questao_numero": 36, "alternativa_correta": "+1" },
    { "questao_numero": 37, "alternativa_correta": "ΔE° = +3,17 V" },
    { "questao_numero": 38, "alternativa_correta": "Ni-Cd, por apresentar o maior número de ciclos." },
    { "questao_numero": 39, "alternativa_correta": "negativo, chamado cátodo." },
    { "questao_numero": 40, "alternativa_correta": "Galvanoplastia: uso de corrente elétrica para depositar uma camada de cromo sobre outro metal." },
    { "questao_numero": 41, "alternativa_correta": "Garantir contraste adequado entre texto e fundo." },
    { "questao_numero": 42, "alternativa_correta": "Um procedimento que ajuda a prevenir a violação de direitos autorais por registrar a origem e a licença da imagem." },
    { "questao_numero": 43, "alternativa_correta": "Reescrever o conteúdo do texto com suas próprias palavras e indicar a fonte da informação." },
    { "questao_numero": 44, "alternativa_correta": "Criar botões de ajuste de tamanho de fonte." },
    { "questao_numero": 45, "alternativa_correta": "botaoDeAcessibilidade.addEventListener('click', function() { opcoesDeAcessibilidade.classList.toggle('apresenta-lista'); });" }
  ];

  // ====== FUNÇÕES ======
  function normalizar(txt) {
    if (!txt) return '';
    return String(txt)
      .toLowerCase()
      .normalize("NFD")
      .replace(/[\u0300-\u036f]/g, "")
      .replace(/\s+/g, " ")
      .trim();
  }

  function atualizarStatus(msg, cor="#ecf0f1") {
    statusDiv.textContent = msg;
    statusDiv.style.color = cor;
  }

  function clicarProximo() {
    const btn = [...document.querySelectorAll("button,a")].find(b =>
      /pr[oó]xima?|avan[cç]ar|continuar|seguinte|prosseguir/i.test(b.textContent.trim())
    );
    if (btn) {
      btn.scrollIntoView({behavior:"smooth", block:"center"});
      setTimeout(()=>btn.click(),300);
    }
  }

  function responderPeloTexto() {
    const numEl = [...document.querySelectorAll("*")].find(el =>
      /quest[ãa]o\s*(\d+)/i.test(el.textContent)
    );
    if (!numEl) return;
    const numero = parseInt(numEl.textContent.match(/quest[ãa]o\s*(\d+)/i)[1]);
    if (numero === ultimoNumeroQuestao) return;
    ultimoNumeroQuestao = numero;

    const questao = gabarito.find(q => q.questao_numero === numero);
    if (!questao) {
      atualizarStatus(`⚠️ Questão ${numero} não no gabarito`, "#e67e22");
      return;
    }

    const correta = normalizar(questao.alternativa_correta);

    // 🔎 procura em <p> com input radio
    const candidatos = [...document.querySelectorAll("p")].filter(el => {
      if (!el.textContent) return false;
      if (!el.querySelector("input[type=radio]")) return false;
      const txt = normalizar(el.textContent);
      return txt.includes(correta) || correta.includes(txt);
    });

    if (candidatos.length === 0) {
      atualizarStatus(`⚠️ Alternativa não encontrada Q${numero}`, "#e67e22");
      return;
    }

    const alvo = candidatos[0];
    alvo.scrollIntoView({behavior:"smooth", block:"center"});
    setTimeout(() => {
      const input = alvo.querySelector('input[type="radio"]');
      if (input && !input.disabled) input.click();
      else alvo.click();
      atualizarStatus(`✅ Questão ${numero} respondida!`, "#27ae60");
      setTimeout(clicarProximo, 800);
    }, 500);
  }

  // ====== PAINEL ======
  const overlay = document.createElement('div');
  overlay.innerHTML = `
    <div style="position:fixed;top:20px;right:20px;background:#2c3e50;color:#fff;
                padding:12px;border-radius:10px;width:300px;font-family:Arial;z-index:99999;
                box-shadow:0 4px 20px rgba(0,0,0,0.5);">
      <h3 style="margin:0 0 10px 0;font-size:16px;display:flex;justify-content:space-between;align-items:center;">
        🤖 AutoResponder
        <button id="fecharBtn" style="background:#e74c3c;border:none;color:#fff;width:24px;height:24px;
                border-radius:50%;cursor:pointer;">✕</button>
      </h3>
      <div id="status" style="margin-bottom:10px;font-size:14px">Aguardando...</div>
      <button id="iniciarBtn" style="background:#27ae60;border:none;color:#fff;padding:8px;
              border-radius:5px;font-weight:bold;width:100%">▶️ Iniciar Automação</button>
      <button id="pararBtn" style="background:#e67e22;border:none;color:#fff;padding:8px;
              border-radius:5px;font-weight:bold;width:100%;margin-top:8px;display:none;">⏹️ Parar</button>
    </div>
  `;
  document.body.appendChild(overlay);

  const statusDiv = overlay.querySelector('#status');
  const iniciarBtn = overlay.querySelector('#iniciarBtn');
  const pararBtn = overlay.querySelector('#pararBtn');
  const fecharBtn = overlay.querySelector('#fecharBtn');

  fecharBtn.addEventListener('click', () => overlay.remove());

  let observando = false;
  let ultimoNumeroQuestao = null;

  function iniciar() {
    if (observando) return;
    observando = true;
    iniciarBtn.style.display = "none";
    pararBtn.style.display = "block";
    responderPeloTexto();
    const observer = new MutationObserver(()=>{ if(observando) responderPeloTexto(); });
    observer.observe(document.body,{childList:true,subtree:true});
    overlay.observer = observer;
    overlay.intervalo = setInterval(()=>{ if(observando) responderPeloTexto(); },2000);
    atualizarStatus("▶️ Automação iniciada","#27ae60");
  }

  function parar() {
    observando = false;
    iniciarBtn.style.display = "block";
    pararBtn.style.display = "none";
    if (overlay.observer) overlay.observer.disconnect();
    if (overlay.intervalo) clearInterval(overlay.intervalo);
    atualizarStatus("⏹️ Automação parada.");
  }

  iniciarBtn.addEventListener('click', iniciar);
  pararBtn.addEventListener('click', parar);

  atualizarStatus("✅ Sistema carregado. Clique em 'Iniciar'.","#27ae60");

})();
